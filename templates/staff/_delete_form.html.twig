<form method="post" action="{{ path('staff_delete', {'staffId': staff.staffId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ staff.staffId) }}">
    <button class="btn">Delete</button>
</form>
