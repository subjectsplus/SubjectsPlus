<form method="post" action="{{ path('media_delete', {'mediaId': media.mediaId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ media.mediaId) }}">
    <button class="btn">Delete</button>
</form>
