<form method="post" action="{{ path('backend_record_delete', {'id': record.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ record.id) }}">
    <button class="btn">Delete</button>
</form>
