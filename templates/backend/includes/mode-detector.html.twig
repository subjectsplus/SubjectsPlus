{# Temporary fix for FART, inline script after body tag #}
{# TODO: Replace once user preference for mode is saved to database #}
<script>
    const currentMode = localStorage.getItem('mode') ? localStorage.getItem('mode') : null;

    if (currentMode) {
        document.documentElement.setAttribute('data-mode', currentMode);
    }
</script>