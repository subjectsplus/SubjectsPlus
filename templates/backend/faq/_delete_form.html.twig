<form method="post" action="{{ path('faq_delete', {'faqId': faq.faqId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ faq.faqId) }}">
    <button class="btn">Delete</button>
</form>
