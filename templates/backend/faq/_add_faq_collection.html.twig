{# TODO: Add working new FAQ collection #}
<div class="modal fade" id="addNewFaqCollection" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="addNewFaqCollectionLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="addNewFaqCollectionLabel">{{ 'control.faqs.admin.new_faq_collection.heading' | trans }}</h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="{{ 'close.aria.label' | trans }}"></button>
            </div>
            <div class="modal-body">
                <form>
                    <div class="form-floating mb-4">
                        <input type="text" class="form-control" id="collectionName" placeholder="{{ 'control.faqs.admin.new_faq_collection.collection_name' | trans }}">
                        <label for="collectionName">{{ 'control.faqs.admin.new_faq_collection.collection_name' | trans }}</label>
                    </div>
                    <div class="text-end">
                        <button type="button" class="btn btn-cancel btn-round me-2" data-bs-dismiss="modal">{{ 'control.faqs.delete_faq.cancel_button' | trans }}</button>
                        <button class="btn btn-gradient btn-round">{{ 'control.faqs.admin.new_faq_collection.add_collection' | trans }}</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>