{% extends 'backend/base.html.twig' %}

{% block browser_title %}{{ 'control.header.faqs.create_new_faq' | trans }}{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('bs_tooltips') }}
{% endblock %}

{% block content %}
    <a href="{{ path('faq_index') }}" class="fs-sm"><i class="fas fa-arrow-alt-circle-left"></i> {{ 'control.faqs.back_to_list' | trans }}</a>
    <h1 class="fw-light">{{ 'control.header.faqs.create_new_faq' | trans }}</h1>

    <div class="row row-cols-lg-2">
        <div class="col-lg-8 text-end">
            {{ include('backend/includes/add_custom_tokens.html.twig') }}
        </div>
    </div>
    <div class="mt-3">
        {{ include('backend/faq/_form.html.twig') }}
    </div>
    {{ include('backend/media/sidebar.html.twig') }}
    {{ include('backend/records/sidebar.html.twig') }}
    {{ include('backend/guide/_offcanvas_booklist.html.twig') }}
{% endblock %}