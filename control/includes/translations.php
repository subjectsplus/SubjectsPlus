<?php
/**
 *   @file control/includes/translations.php
 *   @brief create translation instances for po parser
 *   @description sometimes string that need to be translated are stored as variables
 * 	 and the po parser cannot parse them because they aren't literal strings. In order
 *   to fix this, this file will contain any possible strings that may need to be translated
 *   that are not literal strings.
 *
 *   @author dgonzalez
 *   @date Jan 2013
 *   @todo
 */

_( "New Databases" );
_( "Full Text" );
_( "Openurl" );
_( "Video" );
_( "Audio" );
_( "Mobile Enabled" );
_( "Database Trial" );

?>